import React from 'react';
import { View, Text } from 'react-native';

const Home = ({ navigation }) => {
  return (
    <View>
      <Text>Hello home!</Text>
    </View>
  );
}

export default (Home);
